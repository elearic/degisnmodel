简单工厂模式 (simple factory pattern)
是指由一个工厂对象决定创建出哪一种产品类的实例，但它不属于GOF 23中设计模式。简单工厂适用于 "工厂类负责创建的对象较少的场景"，
且客户端只需要传入工厂类的参数，对于如何创建对象的逻辑不需要关心。


面向产品的工厂


JDK 源代码：

Calendar 类

slf4j.LoggerFactory.getLogger()

缺点：工厂类的职责过于重，不易于扩展过于复杂的产品结构。

适用于固定不容易变化的产品族，比如Calendar